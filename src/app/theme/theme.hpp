#pragma once

namespace app::theme
{
    /**
     * @brief Describes application visual theme settings.
     */
    struct Theme
    {
        bool dark = false;
        int fontSize = 11;
    };

} // namespace app::theme
